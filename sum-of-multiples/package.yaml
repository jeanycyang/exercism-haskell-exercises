name: sum-of-multiples
version: 1.2.0.5

dependencies:
  - base

library:
  exposed-modules: SumOfMultiples
  source-dirs: src
  dependencies:
    - containers

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - sum-of-multiples
      - hspec
